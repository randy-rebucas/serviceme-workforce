import { IntlPhoneValidationDirective } from './intl-phone-validation.directive';

describe('IntlPhoneValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new IntlPhoneValidationDirective();
    expect(directive).toBeTruthy();
  });
});
